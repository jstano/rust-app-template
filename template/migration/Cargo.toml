[package]
name = "{{ crate_prefix }}-migration"
version.workspace = true
edition.workspace = true
publish = false

[lib]
name = "migration"
path = "src/lib.rs"

[[bin]]
name = "{{ crate_prefix }}-migration"
path = "src/main.rs"

[dependencies]
sea-orm-migration = { version = "1.1", features = [
    "sqlx-postgres",
    "runtime-tokio-rustls",
    "cli",
] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
